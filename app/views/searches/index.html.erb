<h1>投稿一覧</h1>
<% @posts.each do|post| %>
<%= link_to post.body, post_path(id: post.id) %>
	<% end %>

<h1>ユーザー一覧</h1>
<% @users.each do|user| %>
<%= link_to user.name, user_path(id: user.id) %>
	<% end %>
